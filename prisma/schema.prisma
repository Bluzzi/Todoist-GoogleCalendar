generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("SQLITE_FILE")
}

model GoogleUser {
  email String @id
  token String
}

model EventSync {
  id String @id @default(uuid())

  googleID  String
  todoistID String

  lastUpdate DateTime
}
